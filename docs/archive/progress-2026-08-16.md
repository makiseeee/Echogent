# 2026-08-16 手机独立运行进展

> 已归档：本文记录当日落地状态，后续变更以当前运维文档为准。

- 手机 Termux/Ubuntu 已常驻运行 AstrBot、Linux QQ 与 NapCat。
- 电脑旧 NapCat 容器已停止，机器人 QQ 仅保留手机登录端。
- 手机重启后可通过打开 Termux 恢复服务；Termux:Boot 仍保留为系统允许时的首选入口。
- ZeroTier SSH 已启用，电脑使用专用 ED25519 公钥连接手机 `10.144.13.0:8022`。
- 手机保存记忆、插件数据库、知识库数据库和向量索引的主副本。
- 电脑 Ollama 仅绑定 `10.144.232.236:11434`，Windows 防火墙只允许手机 `10.144.13.0` 访问。
- 手机 AstrBot 的 `ollama_embedding` 指向电脑 ZeroTier 地址，`bge-m3` 向量维度为 1024。
- self_evolution 每日总结上传失败时写入 `pending_kb_uploads.json`，每 30 分钟自动重试。

## 验收结果

- 手机 AstrBot WebUI 返回 HTTP 200。
- NapCat 单实例运行，OneBot 反向 WebSocket 已连接。
- 拔掉 USB 后 QQ 收发正常。
- 电脑 MCP 端点鉴权正常。
- ZeroTier SSH 公钥登录正常。
- Ollama `/api/embed` 返回 HTTP 200 和 `bge-m3` 向量。
